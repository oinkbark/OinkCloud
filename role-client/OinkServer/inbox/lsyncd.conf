sync {
   default.rsync,
   source = "<MOUNT>/etc/letsencrypt/live/",
   target = "<MOUNT>/etc/letsencrypt/oinkserver/live/",
   rsync = {
     copy_links = true,
     chmod = "0700",
     chown = "www-data:www-data",
     group = true,
     owner = true,
     perms = true
   }
}
